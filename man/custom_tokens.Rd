% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preparer_tokens.R
\name{custom_tokens}
\alias{custom_tokens}
\title{Tokenize with your own tokenizer}
\usage{
custom_tokens(text, tokenizer)
}
\arguments{
\item{text}{A character vector to tokenize.}

\item{tokenizer}{A plain function, or an object with a \code{\link[=tokenize]{tokenize()}} method.}
}
\value{
A list of character vectors, one per input element.
}
\description{
The built-in tokenizers split text into words, n-grams, or dates. When none
of them fits your data, write your own and place it in the strategy formula
with \code{custom_tokens()}:
\code{name ~ normalize_text() + custom_tokens(my_tokenizer)}.
}
\details{
The tokenizer can be a plain function that takes a character vector and
returns a list of token vectors. It can also be a fitted tokenizer object
with a \code{\link[=tokenize]{tokenize()}} method, which is how a tokenizer with learned state,
such as a trained subword vocabulary, enters a strategy.

Whichever form you pass, it must return a list with one element per input
string, each element a character vector of that record's tokens
(\code{character(0)} for a record with none). \code{custom_tokens()} checks this and
stops with a clear error if the result does not fit.
}
\examples{
# A bare-function tokenizer: split on vowels
split_vowels <- function(text) strsplit(text, "[aeiou]+")
custom_tokens(c("plexiglas", "brandenburg"), split_vowels)

}
\seealso{
\code{\link[=tokenize]{tokenize()}}, the method a fitted tokenizer provides;
\code{\link[=word_tokens]{word_tokens()}} for the standard whitespace tokenizer.

Other token generators: 
\code{\link{generate_ngrams}()},
\code{\link{numeric_tokens}()},
\code{\link{word_tokens}()}
}
\concept{token generators}
