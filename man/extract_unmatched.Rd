% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_core.R
\name{extract_unmatched}
\alias{extract_unmatched}
\title{Extract Unmatched Records}
\usage{
extract_unmatched(data, id, matches, ...)
}
\arguments{
\item{data}{A data.frame / tibble / data.table (or db table in other backends)
containing the original records.}

\item{id}{Character scalar naming the ID column in \code{data}.}

\item{matches}{A table of matched record pairs, containing the ID column.}

\item{...}{Additional arguments passed to backend-specific methods.}
}
\value{
A subset of \code{data} containing only records whose IDs do not appear
in \code{matches}.
}
\description{
Identify and extract records from a table that were
not matched in a record linkage operation.
}
\examples{
strat <- search_strategy(
  workshop ~ normalize_text() + word_tokens(min_nchar = 3),
  block_by  = c("postcode_area", "trade"),
  threshold = 0.7
)
matches <- search_candidates(
  workshop_listings, workshop_register,
  base_id = "listing_id", target_id = "reg_no", strategy = strat
)
# The listings that found no register match, ready for a looser next pass.
leftover <- extract_unmatched(workshop_listings, "listing_id", matches)
nrow(leftover)

}
