% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding_cache.R
\name{clear_embedding_cache}
\alias{clear_embedding_cache}
\title{Clear the embedding reuse cache}
\usage{
clear_embedding_cache(disk = FALSE)
}
\arguments{
\item{disk}{Logical. If \code{TRUE}, also delete the on-disk cache files in the
directory set by \code{options(joinery.embedding_cache_dir = ...)}. Defaults to
\code{FALSE} (clear the in-session cache only).}
}
\value{
Invisibly \code{NULL}.
}
\description{
Empties joinery's in-session embedding cache, and optionally the on-disk
cache. The cache stores raw embedding vectors so that the data.table and
tibble backends reuse them instead of re-embedding on every call. You rarely
need to call this by hand; it is mainly useful to force a clean re-embed or to
reclaim memory in a long-running session.
}
\seealso{
\code{\link[=compute_embeddings]{compute_embeddings()}} for how the cache is filled, and
\code{joinery} (package options) for \code{joinery.embedding_reuse} and
\code{joinery.embedding_cache_dir}.
}
