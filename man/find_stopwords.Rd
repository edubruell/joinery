% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preparer_stopwords.R
\name{find_stopwords}
\alias{find_stopwords}
\title{Discover candidate stopwords from a prepared token table}
\usage{
find_stopwords(
  tokens,
  max_prop = 0.3,
  top_n = NULL,
  by_block = FALSE,
  block_by = NULL
)
}
\arguments{
\item{tokens}{A token table produced by \code{\link[=prepare_search_data]{prepare_search_data()}}
(data.table or DuckDB backend). Must contain \code{src_column}, \code{token}, and
\code{row_id}.}

\item{max_prop}{Numeric in \verb{(0, 1]}. Return tokens whose document-frequency
share is at least this value. Default \code{0.3} (token appears in ≥30\% of a
column's records).}

\item{top_n}{Optional integer. If supplied, instead of (or in addition to)
the \code{max_prop} cut, keep at most the \code{top_n} most frequent tokens per
column. When both are given, the union is returned.}

\item{by_block}{Logical. Compute the share within each block rather than
corpus-wide. Requires \code{block_by} to name the block columns (the token
table also carries the id column, so they cannot be inferred safely).
Default \code{FALSE}.}

\item{block_by}{Character vector of block columns. Required when
\code{by_block = TRUE}; pass the strategy's \code{block_by}. Ignored otherwise.}
}
\value{
A \code{data.table} with one row per flagged \verb{(src_column, token)}:
\code{src_column}, \code{token}, \code{df} (distinct records containing the token),
\code{n_records} (records in the column / block), and \code{prop = df / n_records}.
Sorted by \code{src_column} then descending \code{prop}. Empty (zero-row) when
nothing crosses the threshold.
}
\description{
Scores every \verb{(src_column, token)} by its document frequency - the share of
records in that column whose value contains the token - and returns the
tokens common enough to be poor discriminators. These are stopword
candidates: feed them to \code{\link[=filter_stopwords]{filter_stopwords()}} in the preparer chain and
re-run \code{prepare_search_data()}.
}
\details{
Document frequency is computed corpus-wide by default (\code{by_block = FALSE}),
i.e. across all blocks. This matches the intuition of a stopword as a
globally common term. With \code{by_block = TRUE} the share is computed within
each block and a token is returned if it crosses \code{max_prop} in \emph{any} block,
reported at its maximum block-level share - useful when a token is rare
overall but saturates a single dense block.
}
\seealso{
\code{\link[=filter_stopwords]{filter_stopwords()}} to apply the result in a preparer chain.
}
