% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_functions.R
\name{search_candidates}
\alias{search_candidates}
\title{Search for Matching Candidates}
\usage{
search_candidates(
  .base_table,
  .target_table,
  .base_key,
  .target_key,
  .threshold,
  .weights = NULL,
  .chunksize = 0
)
}
\arguments{
\item{.base_table}{A data table containing the base records, with tokens and columns.}

\item{.target_table}{A data table containing the target records, with tokens and columns.}

\item{.base_key}{A character string specifying the key column in the base table.}

\item{.target_key}{A character string specifying the key column in the target table.}

\item{.threshold}{A numeric value specifying the minimum identification potential required to consider a match.}

\item{.weights}{A named numeric vector specifying the weights for each column. If NULL, equal weights are used.}

\item{.chunksize}{An integer specifying the number of target records to process in each chunk. Set to 0 for no chunking.}
}
\value{
A data.table containing the pairs of matched records with their identification potential.
}
\description{
This function searches for matching candidates between a base table and a target table using
token-based heuristic linkage. It calculates the identification potential for each pair of records
based on the rarity of shared tokens and specified weights.
}
\examples{
candidates <- search_candidates(
  .base_table = search_table_base,
  .target_table = search_table_target,
  .base_key = "key_base",
  .target_key = "key_target",
  .threshold = 0.6,
  .weights = c(Hausnummer = 0.1, Nachname = 0.5, Vorname = 0.2, Strasse = 0.1, Ort = 0.1),
  .chunksize = 10000
)
}
