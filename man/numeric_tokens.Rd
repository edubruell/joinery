% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preparers_datatable.R
\name{numeric_tokens}
\alias{numeric_tokens}
\title{Tokenize numeric fields, expanding ranges into individual numbers}
\usage{
numeric_tokens(text, keep_letters = TRUE, destructive = FALSE)
}
\arguments{
\item{text}{Character vector of numeric or address fields.}

\item{keep_letters}{Logical. If TRUE, retains letter suffixes like "12A".
Only applies when `destructive = FALSE`.}

\item{destructive}{Logical. If TRUE, removes all non-digit characters
except whitespace. If FALSE (default), preserves letters alongside digits.}
}
\value{
A list of character vectors, one per input element. Each vector
  contains numeric tokens, with ranges expanded into sequences.
}
\description{
Turns numeric/house-number–like text into a list of tokens.
Expands ranges such as "12-14" or "7–9" into c("12","13","14").
Uses original spacing/separators to detect ranges, while normalization
cleans text for tokenization.
}
\examples{
numeric_tokens("12-14")
# list(c("12", "13", "14"))

numeric_tokens("7A 9B", keep_letters = TRUE)
# list(c("7A", "9B"))

numeric_tokens("House 5", destructive = TRUE)
# list("5")

}
