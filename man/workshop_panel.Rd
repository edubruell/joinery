% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{workshop_panel}
\alias{workshop_panel}
\title{Multi-year workshop panel for cross-year linkage examples}
\format{
A tibble with 847 rows and 10 variables:
\describe{
\item{record_id}{Character row id, unique per workshop-year (e.g. "YR-00042")}
\item{year}{Observation year, 2019 to 2023}
\item{workshop}{Business name as recorded that year, with light per-year noise}
\item{proprietor}{Proprietor name, in a directory rendering that varies by year}
\item{trade}{One of eight woodworking trades}
\item{postcode_area}{UK outward-code area; changes mid-trajectory for movers}
\item{town}{Town for the postcode area}
\item{established}{Year the workshop was established (stable within an entity)}
\item{true_entity}{Evaluation only. The stable entity key (the
\code{workshop_register} \code{reg_no}); every year-row of one workshop
shares it.}
\item{change_tier}{Evaluation only. The cross-year challenge the entity
carries: \code{stable} (per-year noise only), \code{name_drift} (a
structural name change part-way through), \code{mover} (a postcode-area
relocation), or \code{phonetic} (a code-preserving stem twin).}
}
}
\source{
Synthetically generated by \code{data-raw/generate_workshop_panel.R},
which draws core workshops from \code{workshop_register} and gives each a
year span with drift. Seeded and offline; ships no real business.
}
\usage{
workshop_panel
}
\description{
A small pooled-long panel of the same UK woodworking workshops as
\code{workshop_register}, observed across five years (2019 to 2023). It is the
runnable example for cross-year entity resolution: each row is one workshop in
one year, names drift over time, a minority of workshops relocate to a
different postcode area, and workshops enter and exit so trajectories have
gaps. The task is to recover the stable identity (\code{true_entity}) behind
the year-by-year rows. A reappearance window of five years matches the design
of the real Yellow Pages panel the package was built against.
}
\seealso{
\code{\link{workshop_register}}, \code{\link{workshop_listings}}
}
\keyword{datasets}
