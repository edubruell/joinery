% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_core.R
\name{deduplicate_table}
\alias{deduplicate_table}
\title{Deduplicate a Table}
\usage{
deduplicate_table(base_table, duplicates, id, ...)
}
\arguments{
\item{base_table}{A data.frame / tibble / data.table (or db table in other backends).}

\item{duplicates}{A table of duplicate pairs generated by detect_duplicates}

\item{id}{Character scalar naming the ID column in \code{base_table}.}

\item{...}{Additional arguments passed to backend-specific methods.}
}
\value{
A deduplicated version of \code{base_table}.
}
\description{
Generic function that removes or merges duplicate records from a table
based on duplicate pairs identified by \code{detect_duplicates()}.
}
\examples{
ex <- exact_strategy(
  workshop ~ normalize_text() + word_tokens(min_nchar = 3),
  block_by = c("postcode_area", "trade")
)
dups <- detect_duplicates(workshop_register, id = "reg_no", strategy = ex)
deduped <- deduplicate_table(workshop_register, dups, id = "reg_no")
c(before = nrow(workshop_register), after = nrow(deduped))

}
