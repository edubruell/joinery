% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_core.R
\name{tokenize}
\alias{tokenize}
\title{Tokenize Text with a Fitted Tokenizer}
\usage{
tokenize(tokenizer, text, ...)
}
\arguments{
\item{tokenizer}{A fitted tokenizer object.}

\item{text}{A character vector, one element per record.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A list of character vectors, one per element of \code{text}.
}
\description{
Most tokenizers in joinery are plain functions, like \code{\link[=word_tokens]{word_tokens()}}. Some
tokenizers instead carry fitted state, for example a subword vocabulary
learned from your corpus. Such a tokenizer is an object, and \code{tokenize()}
is how that object turns text into tokens. You rarely call it yourself:
place the object in a strategy formula with \code{\link[=custom_tokens]{custom_tokens()}} and joinery
calls \code{tokenize()} for you.

To bring your own tokenizer, write a \code{tokenize()} method for its class
with \code{S7::method()}. The method must return a list with one element per
element of \code{text}, each element a character vector of that record's
tokens (\code{character(0)} for a record with none).
}
\seealso{
\code{\link[=custom_tokens]{custom_tokens()}}, which puts a tokenizer into a strategy
formula.
}
