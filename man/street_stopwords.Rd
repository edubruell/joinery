% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{street_stopwords}
\alias{street_stopwords}
\title{Multilingual Street-Name Stopwords}
\format{
An object of class \code{tbl_df} (inherits from \code{tbl}, \code{data.frame}) with 58 rows and 2 columns.
}
\source{
Manually curated from common multi-word street-name patterns across
languages. Expandable as new particles are encountered.
}
\usage{
street_stopwords
}
\description{
Locative particles and articles that recur inside multi-word street names
but carry no discriminative signal for matching - German \verb{AN DER}, French
\verb{DE LA}, Italian \code{DELLA}, and so on. Used by \code{\link[=normalize_street]{normalize_street()}} when
\code{drop_stopwords = TRUE} to collapse e.g. \code{"An der Alster"} to \code{"ALSTER"}.
}
\details{
The list is deliberately tight: only true prepositions and articles, never
adjectives (\code{"NEUE"}, \code{"GROSSE"}) or directionals that can themselves be the
distinguishing part of a name. Entries are uppercase ASCII so they join
directly against \code{normalize_street()}'s already-uppercased, transliterated
tokens. When a \code{lang} is supplied to \code{normalize_street()}, only that
language's particles are removed.
\subsection{Format}{

A tibble with two columns:

\describe{
\item{stopword}{Character string. The particle in uppercase ASCII
(e.g. \code{"AN"}, \code{"DER"}, \code{"DE"}, \code{"DELLA"}).}
\item{lang}{ISO 639-1 language code (\code{"de"}, \code{"en"}, \code{"fr"}, \code{"es"},
\code{"it"}, \code{"pt"}, \code{"nl"}).}
}
}
}
\seealso{
\code{\link[=normalize_street]{normalize_street()}}, \link{street_types}
}
\keyword{datasets}
