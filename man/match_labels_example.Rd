% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{match_labels_example}
\alias{match_labels_example}
\title{Labelled candidate pairs for calibration examples}
\format{
A data frame with 1,862 rows (two rows per pair) and 12 variables:
\describe{
\item{match_id}{Integer pair id. Each id groups one \code{base} row and one
\code{target} row.}
\item{score}{The candidate score from the frozen search}
\item{source}{\code{"base"} (the searched listing) or \code{"target"} (the
register candidate). The candidate row carries the label.}
\item{id}{The record id: a \code{listing_id} on base rows, a \code{reg_no}
on target rows}
\item{workshop}{Business name of the row}
\item{proprietor}{Proprietor name of the row}
\item{trade}{Trade}
\item{postcode_area}{UK outward-code area}
\item{gen_tier}{The generation tier of the row, useful for slicing the
hard cases}
\item{actual_link}{The searched listing's true \code{reg_no} (present on
base rows)}
\item{rank}{Candidate rank within the pair}
\item{equal}{Evaluation label. \code{1} when the target candidate is the
listing's true match, \code{0} otherwise. On base header rows it is the
block default.}
}
}
\source{
Synthetically generated by \code{data-raw/generate_match_labels.R}
from the shipped \code{workshop_listings} / \code{workshop_register} pair.
}
\usage{
match_labels_example
}
\description{
A frozen set of candidate match pairs with a ground-truth \code{equal} label,
for the calibration workflow (\code{\link{fit_filter}},
\code{\link{apply_filter}}, \code{\link{calibrate}}). It is a saved
\code{search_candidates()} run linking \code{workshop_listings} to
\code{workshop_register}, with \code{equal} filled from each listing's true
\code{actual_link}. The false positives are concentrated in the deliberately
hard tiers, common-surname homonyms and planted register duplicates, so a
learned filter has real mistakes to catch. The schema matches what
\code{\link{import_labels}} returns, so it feeds the calibration verbs with no
manual labelling step.
}
\seealso{
\code{\link{fit_filter}}, \code{\link{import_labels}},
\code{\link{workshop_listings}}
}
\keyword{datasets}
