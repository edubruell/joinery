% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_diagnostic.R
\name{sample_matches}
\alias{sample_matches}
\title{Sample Matches for Review}
\usage{
sample_matches(matches, ...)
}
\arguments{
\item{matches}{Match output table.}

\item{...}{Method-specific arguments. Standard arguments: \code{mode}
(one of the sampling modes above), \code{n} (number of rows to sample),
and mode-specific extras (e.g. \code{threshold} for \code{"borderline"}).}
}
\value{
A \code{Match_Sample} object.
}
\description{
Sampling diagnostic (Q4). Modes: \code{"high"}, \code{"low"},
\code{"borderline"}, \code{"ambiguous"}, \code{"top_gap"}, \code{"random"}.
}
\examples{
\dontshow{data.table::setDTthreads(2)}
strat <- search_strategy(
  workshop ~ normalize_text() + word_tokens(min_nchar = 3),
  block_by  = c("postcode_area", "trade"),
  threshold = 0.7
)
matches <- search_candidates(
  workshop_listings, workshop_register,
  base_id = "listing_id", target_id = "reg_no", strategy = strat
)
# Pull the borderline pairs near the threshold, the ones worth eyeballing.
sample_matches(matches, mode = "borderline", n = 5, threshold = 0.7)

}
