% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joinery-package.R
\docType{package}
\name{joinery-package}
\alias{joinery}
\alias{joinery-package}
\title{joinery: Heuristic Index-Based Record Linkage in R}
\description{
\if{html}{\figure{logo.png}{options: style='float: right' alt='logo' width='120'}}

Index-based heuristic record linkage for R.
}
\section{Package options}{


joinery reads a small number of global options. Set them with
\code{\link[=options]{options()}}; all have working defaults, so you only touch them to change
behaviour.

Embedding strategies embed each record once and reuse the vector on later
calls, so a multi-stage run does not pay the (expensive) embedding cost again.
Two options control that reuse:

\describe{
\item{\code{joinery.embedding_reuse}}{Logical, default \code{TRUE}. When \code{TRUE}, the
data.table and tibble backends keep a per-session cache of embedding
vectors keyed by model and record content, and reuse a vector whenever the
same text is embedded again. Set to \code{FALSE} to embed fresh every time, for
example when benchmarking or using a non-deterministic model. (The DuckDB
backend reuses through its own persisted \code{embeddings} column and ignores
this option.) The session cache grows as you embed more distinct records
and is only released at the end of the session or by
\code{\link[=clear_embedding_cache]{clear_embedding_cache()}}; call that to reclaim memory in a long-running
session.}
\item{\code{joinery.embedding_cache_dir}}{Character path, default unset (\code{NULL}).
When set, the embedding cache also writes each vector to this directory, so
reuse survives across R sessions. When unset, the cache lives only in the
current session. The cache is keyed by record content, so a changed record
re-embeds on its own; clear stale files with
\code{\link[=clear_embedding_cache]{clear_embedding_cache()}} (\code{disk = TRUE}).}
}
}

\seealso{
\code{\link[=clear_embedding_cache]{clear_embedding_cache()}}, \code{\link[=embedding_strategy]{embedding_strategy()}}
}
\author{
\strong{Maintainer}: Eduard Brüll \email{eduard.bruell@zew.de}

}
\keyword{internal}
